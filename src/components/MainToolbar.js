import "../css/maintoolbar.css";

const MainToolbar = () => {
  return (
    <div id="main-toolbar">
      <button>Pen</button>
      <button>Dot</button>
      <button>Eraser</button>
    </div>
  );
};

export default MainToolbar;
